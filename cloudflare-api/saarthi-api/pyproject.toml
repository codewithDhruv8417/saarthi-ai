[project]
name = "saarthi-api"
version = "0.1.0"
description = "SaarthiAI citizen grievance AI service"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "httpx",
    "python-multipart>=0.0.32",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
